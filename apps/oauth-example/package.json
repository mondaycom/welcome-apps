{
  "name": "monday-oauth-example",
  "version": "1.0.0",
  "description": "An example of a server-side monday app that uses OAuth2",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run stop && node index.js",
    "expose": "ngrok http 8306",
    "stop": "kill-port 8306 && kill-port 4049 && kill-port 4040"
  },
  "author": "Dipro Bhowmik",
  "license": "BSD",
  "dependencies": {
    "concurrently": "^5.3.0",
    "cookie-parser": "^1.4.4",
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "kill-port": "^1.6.1",
    "querystring": "^0.2.0",
    "request": "^2.88.2"
  }
}
